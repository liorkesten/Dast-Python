def Dijkstra(g, w, s):
    """
    Dijkstra's algorithm - find the "shorter" way from s to to each node.

                    Time Complexity: O((|V|+|E|)*(log|E|))
    :param g: undirected graph.
    :param w: Weight function
    :param s: Vertex that in g
    :return: Array of distances from s to each node
    (infinity - there is not route from s to that vertex)
    """
    pass
